#!/usr/bin/python3


def number_keys(a_dictionary: dict):
    """finds the number of keys in a dictionary

    Args:
        a_dictionary: the dictionary whose key count we want

    Return:
        number of keys in dictionary
    """
    return len(a_dictionary.keys())
